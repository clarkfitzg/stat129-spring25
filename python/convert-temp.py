celsius = 0
fahrenheit = celsius * 1.8 + 32

# Put the above code into a function named c_to_f
# that accepts the degrees in celsius as an argument and 
# returns the degrees in fahrenheit.
# Note: use the exact name c_to_f
